# 关于外参标定的方法与注意事项
## 1.环境准备
### 版本
PCL>=1.8, OpenCV>=4.0. ubuntu=22.04,ros2
### 下载
1. docker
`由于该算法需要在ros1环境下运行，所以需要用docker这个虚拟容器下载ros1来运行`
下载：请参考https://blog.csdn.net/2302_80099075/article/details/147169072
2. 海康相机的平台
https://blog.csdn.net/2302_80099075/article/details/147169072
假如是nx,应该下载ARM架构
3. 相机驱动，雷达驱动与融合算法
请参考我的另一个仓库
- [ 还未上传 ]
使用时请参考一下这两篇的README以免出现不必要的问题
4. 其他的请在操作过程中逐步下载
## 2.内参标定
请参考
（https://www.mubu.com/doc/7OyJ2R_aGmV）
找到“内参标定”按照步骤来得到内参标定结果
> 里面的biaoding.sh用MVS.sh来代替，等海康相机的平台下好之后，按顺序执行
```
cd /opt/MVS/bin
./MVS.sh
```
可以使用海康平台来拍摄
## 3.录制rosbag
打开相机驱动，雷达驱动与融合算法的资源，检查相机与雷达是否连接好，然后编译，在两个终端运行雷达与相机驱动
```
ros2 launch camera camera.launch.py
ros2 launch livox_ros_driver2 livox_lidar_rviz.launch.py
#大概是这样，具体名字请查看launch文档
```
使用
```
ros2 topic list
```
观察终端输出，看到`\image_raw \livox_lidar`验证是否打开了这两个驱动
然后录制rosbag
```
timeout 10s ros2 bag record /image_raw /livox_lidar -o my_bag 
```
录制好后，会出现一个文件夹，里面有一个配置文件和一个db3的文件
使用
```
pip install --user rosbags
# 将 ROS 2 目录（含 metadata.yaml 与 *.db3）转换为 ROS 1 bag，输出到 out_ros1 目录
rosbags-convert --dst out_ros1 /你的rosbag路径
```
## 4.外参标定算法
请严肃参考下面的
`TUTORIAL.md`
按照下载面的教程来标定
`可能遇到的问题`
- 算法识别不出来圆孔，要怎么做
（按照优先级来修改）
1. 修改`lidar_detect.hpp`
```
#注意修改下面的东西
ec.setClusterTolerance(0.03); // 设置聚类距离阈值，可以改大一点
ec.setMinClusterSize(20);     // 最小点数
ec.setMaxClusterSize(5000);   // 最大点数

if (error < 0.04)  //也改大一点

```
2. 对录制包里的点云进行反射率过滤
请用ai写一个反射率的过滤算法，将包里的点云低于一定域值的点过滤掉，输出过滤后的点，可以一定程度上解决边缘膨胀带来的噪声，把这个包用来跑跑，看看行不行的通
3. 内参或者录制时的问题，需要进一步检查数据的质量和内参标定时的步骤
## 5.运行融合算法
打开相机驱动，雷达驱动与融合算法的文件夹，在两个终端运行雷达与相机驱动，然后运行融合的算法
```
ros2 run lidar_camera_fusion lidar_camera_fusion.node
```
打开新终端，运行`rviz2`,在 `fixed frame`中输入`lidar_frame`，打开底部的`add`,点击`by topic`,打开彩色点云的point_cloud2,修改`style`为point,修改`Decay time`长一些，效果好的话可以看到上色后的点云（尽量在你要看的位置放上一个颜色鲜艳的东西，验证外参的准确性）

***
